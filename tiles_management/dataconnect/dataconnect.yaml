specVersion: "v1beta"
serviceId: "tiles_management"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tiles_management-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
